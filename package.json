{
  "name": "quickink-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter user-app --filter admin-app dev",
    "dev:user": "pnpm --filter user-app dev",
    "dev:admin": "pnpm --filter admin-app dev",
    "desktop:dev": "concurrently --kill-others --names \"API,ELECTRON\" --prefix-colors \"cyan,yellow\" \"pnpm --filter user-app dev\" \"wait-on http://localhost:3000 && pnpm --filter quickink-desktop start\"",
    "desktop:start": "concurrently --kill-others --names \"API,ELECTRON\" --prefix-colors \"cyan,yellow\" \"pnpm --filter user-app dev\" \"wait-on http://localhost:3000 && pnpm --filter quickink-desktop start\"",
    "desktop:install": "pnpm --filter quickink-desktop install",
    "build": "pnpm --filter user-app --filter admin-app build",
    "build:user": "pnpm --filter user-app build",
    "build:admin": "pnpm --filter admin-app build"
  },
  "dependencies": {
    "concurrently": "^10.0.5",
    "wait-on": "^9.1.0"
  }
}
